<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="12dp">

    <!-- Paper Container -->
    <LinearLayout
        android:id="@+id/widget_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@drawable/widget_paper_bg"
        android:padding="16dp"
        android:clickable="true"
        android:focusable="true">

        <!-- Header -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/widget_title"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="REMI"
                android:textColor="#1A1A1A"
                android:textSize="12sp"
                android:textStyle="bold"
                android:fontFamily="sans-serif-medium" />

            <View
                android:layout_width="6dp"
                android:layout_height="6dp"
                android:background="@drawable/pulse_button_bg" />

        </LinearLayout>

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="#E5E3DE"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="12dp" />

        <!-- Centered Focus Thought -->
        <TextView
            android:id="@+id/widget_memory_text"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:text="Tippe zum Aufnehmen..."
            android:textColor="#3A3A3A"
            android:textSize="18sp"
            android:gravity="center"
            android:fontFamily="sans-serif"
            android:maxLines="3"
            android:ellipsize="end" />

        <!-- Action Area -->
        <FrameLayout
            android:id="@+id/btn_quick_capture"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="end"
            android:layout_marginTop="12dp"
            android:background="@drawable/pulse_button_bg"
            android:clickable="true"
            android:focusable="true">

            <!-- The "In-App Dot" icon -->
            <View
                android:layout_width="10dp"
                android:layout_height="10dp"
                android:layout_gravity="center"
                android:background="#99FFFFFF" />
        </FrameLayout>

    </LinearLayout>
</FrameLayout>
